export const MAX_FILE_SIZE = Math.pow(2048, 2);

// Session
export const SESSION_PREFIX = 'telemedicine-session:';
export const COOKIE_NAME = 'session_token';
export const COOKIE_EXPIRATION = 1000 * 60 * 60 * 24 * 15; // 15 days
